// Copyright 2026 smashingtags. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newServicedeskapiGetRequestCommentsCmd(flags *rootFlags) *cobra.Command {
	var flagPublic bool
	var flagInternal bool
	var flagExpand string
	var flagStart int
	var flagLimit int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-request-comments <issueIdOrKey>",
		Short:       "This method returns all comments on a customer request.",
		Example:     "  jira-pp-cli-pp-cli servicedeskapi get-request-comments your-token-here",
		Annotations: map[string]string{"pp:endpoint": "servicedeskapi.get-request-comments", "pp:method": "GET", "pp:path": "/rest/servicedeskapi/request/{issueIdOrKey}/comment", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/rest/servicedeskapi/request/{issueIdOrKey}/comment"
			path = replacePathParam(path, "issueIdOrKey", args[0])
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "servicedeskapi", path, map[string]string{
				"public":   fmt.Sprintf("%v", flagPublic),
				"internal": fmt.Sprintf("%v", flagInternal),
				"expand":   fmt.Sprintf("%v", flagExpand),
				"start":    fmt.Sprintf("%v", flagStart),
				"limit":    fmt.Sprintf("%v", flagLimit),
			}, nil, flagAll, "", "offset", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagPublic, "public", false, "Specifies whether to return public comments or not. Default: true.")
	cmd.Flags().BoolVar(&flagInternal, "internal", false, "Specifies whether to return internal comments or not. Default: true.")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "A multi-value parameter indicating which properties of the comment to expand")
	cmd.Flags().IntVar(&flagStart, "start", 0, "The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "The maximum number of comments to return per page. Default: 50.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
