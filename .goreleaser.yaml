version: 2
project_name: jira-pp-cli-pp-cli
changelog:
  disable: true
builds:
  - id: jira-pp-cli-pp-cli
    main: ./cmd/jira-pp-cli-pp-cli
    binary: jira-pp-cli-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X jira-pp-cli-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: jira-pp-cli-pp-mcp
    main: ./cmd/jira-pp-cli-pp-mcp
    binary: jira-pp-cli-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: jira-pp-cli-pp-cli
    repository:
      owner: smashingtags
      name: homebrew-tap
    homepage: "https://github.com/smashingtags/jira-pp-cli-pp-cli"
    description: "Combined CLI for multiple API services"
    install: |
      bin.install "jira-pp-cli-pp-cli"
      bin.install "jira-pp-cli-pp-mcp"
